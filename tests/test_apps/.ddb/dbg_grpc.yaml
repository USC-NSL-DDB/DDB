---
Framework: grpc

FrameFilter:
  filter_preset:
    - "cpp-stdlib"
    - "grpc"

PrerunGdbCommands:
  - name: "Pretty Print for CPP"
    command: "-enable-pretty-printing"

ServiceDiscovery:
  Broker:
    hostname: 127.0.0.1
    managed:
      type: mosquitto

Conf:
  on_exit: kill
